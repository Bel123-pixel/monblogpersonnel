{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && php artisan db:seed --class=AdminSeeder --force && php artisan storage:link || true && php -S 0.0.0.0:${PORT:-8080} -t public"
  }
}
